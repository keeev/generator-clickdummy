language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-clickdummy' ]; then cd .. && eval "mv $currentfolder generator-clickdummy" && cd generator-clickdummy; fi

