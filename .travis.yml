language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-Pulpmedia' ]; then cd .. && eval "mv $currentfolder generator-Pulpmedia" && cd generator-Pulpmedia; fi

